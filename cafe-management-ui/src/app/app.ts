import { Component, signal } from '@angular/core';
import { Layout } from './layout';

@Component({
  selector: 'app-root',
  templateUrl: './app.html',
  styles: [],
  imports: [Layout],
})
export class App {}
